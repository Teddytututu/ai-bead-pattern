{
  "name": "@ai-bead-pattern/ai-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @ai-bead-pattern/pattern-core build && tsc -p tsconfig.test.json && node --test .test-dist/test/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-bead-pattern/pattern-core": "workspace:*",
    "sharp": "^0.35.3"
  }
}
