{
  "name": "@ai-bead-pattern/pattern-core",
  "version": "0.6.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./experimental": {
      "types": "./dist/experimental.d.ts",
      "default": "./dist/experimental.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "benchmark": "pnpm build && node examples/benchmark.mjs",
    "benchmark:shape": "pnpm build && node examples/shape-benchmark.mjs",
    "benchmark:max": "pnpm build && node examples/benchmark.mjs --max",
    "example": "pnpm build && node examples/basic.mjs",
    "test": "tsc -p tsconfig.test.json && node --test .test-dist/test/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
